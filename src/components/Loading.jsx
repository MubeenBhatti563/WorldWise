import React from "react";

const Loading = () => {
  return (
    <div className="bg-slate-900 text-4xl text-white flex items-center justify-center h-screen w-screen">
      <p>Loading...</p>
    </div>
  );
};

export default Loading;
